import { t } from "@/content/ar";

export const whatsappWidget = {
  agentName: t.whatsappWidget.agentName,
  agentTitle: t.whatsappWidget.agentTitle,
  greeting: t.whatsappWidget.greeting,
  subGreeting: t.whatsappWidget.subGreeting,
  quickMessagesTitle: t.whatsappWidget.quickMessagesTitle,
  quickMessages: t.whatsappWidget.quickMessages,
  startChat: t.whatsappWidget.startChat,
  orCall: t.whatsappWidget.orCall,
} as const;